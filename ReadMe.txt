Important notes.

Must set the KAFKA_HOME in both the "start-kafka.sh" and "start-zookeeper.sh"


Running the app
-first run the Zookeeper
-next run the server

-if we are running for the first time have to register the topics with Zookeeper

-next run the producer
-next run the consumers